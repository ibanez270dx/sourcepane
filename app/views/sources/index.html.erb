<h1>Sources#index</h1>
<p>Find me in app/views/sources/index.html.erb</p>

<% Source::TYPES.each do |type| %>
  <% unless @sources.include?(type.downcase) %>
    <%= link_to type.titleize, new_source_path(type: type) %>
  <% end %>
<% end %>
